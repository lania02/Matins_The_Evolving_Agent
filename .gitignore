# Matins runtime artifacts
data/
*.db
*.db-journal
*.log

# Local config with secrets / chat ids
config.yaml

# Per-user local Claude Code settings (keep shared .claude/settings.json if added)
.claude/settings.local.json

# Personal curated-ideas mirror (derived from the DB; contains your research ideas)
favorites.md

# On-demand deep-dive briefings (derived; contain your research ideas)
deep_dives/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Test / tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Optional Node bridge
bridges/whatsapp_baileys/node_modules/
